# MentalSpace EHR - Production Environment Template
# Copy this file to .env in production and fill in actual values
# NEVER commit .env to git - it should be in .gitignore

# Supabase Configuration (provided by Lovable Cloud)
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_key_here
VITE_SUPABASE_PROJECT_ID=your_project_id_here

# Application Settings
VITE_APP_NAME=MentalSpace EHR
VITE_APP_ENV=production
VITE_APP_VERSION=1.0.0

# Features Flags (optional)
VITE_ENABLE_AI_NOTES=true
VITE_ENABLE_TELEHEALTH=true
VITE_ENABLE_BILLING=true
VITE_ENABLE_PORTAL=true

# External Service URLs (optional - for whitelabeling)
VITE_SUPPORT_EMAIL=support@yourpractice.com
VITE_SUPPORT_PHONE=+1-555-555-5555

# Analytics (optional)
VITE_ENABLE_ANALYTICS=false
VITE_ANALYTICS_ID=

# Monitoring (optional)
VITE_SENTRY_DSN=
VITE_ENABLE_ERROR_TRACKING=false

# IMPORTANT NOTES:
# 1. All Supabase secrets (service keys, API keys) are managed in Supabase Edge Functions
# 2. Never put sensitive keys in environment variables prefixed with VITE_ (they're exposed to client)
# 3. Use Supabase Secrets for: OPENAI_API_KEY, TWILIO_*, RESEND_API_KEY, etc.
# 4. Only public/publishable keys should be in this file
